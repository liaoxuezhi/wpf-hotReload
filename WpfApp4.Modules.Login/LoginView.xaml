<UserControl x:Class="WpfApp4.Modules.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#f5f5f5">
        <Border Width="320" Height="240" Background="White" CornerRadius="8"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderBrush="#dddddd" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel Margin="10" VerticalAlignment="Center">
                <TextBlock Text="用户登录" FontSize="22" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,25"/>
                
                <TextBlock Text="用户名" Margin="0,0,0,5" Foreground="#666"/>
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" 
                         Height="30" Margin="0,0,0,12" Padding="5,3" BorderBrush="#ccc"/>
                
                <TextBlock Text="密码" Margin="0,0,0,5" Foreground="#666"/>
                <PasswordBox x:Name="PasswordBox" Height="30" Margin="0,0,0,20" Padding="5,3" BorderBrush="#ccc"/>
                
                <Button Content="登 录" Click="LoginButton_Click"
                        Height="34" Background="#0078d4" Foreground="White" 
                        BorderThickness="0" FontSize="14" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
