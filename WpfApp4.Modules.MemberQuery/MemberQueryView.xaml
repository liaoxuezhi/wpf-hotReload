<UserControl x:Class="WpfApp4.Modules.MemberQuery.MemberQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid>
            <TextBlock Text="会员查询" FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Button Content="退出登录" Command="{Binding LogoutCommand}" 
                    HorizontalAlignment="Right" VerticalAlignment="Center"
                    Width="80" Height="28" Background="#e74c3c" Foreground="White" 
                    BorderThickness="0" FontSize="12" Cursor="Hand"/>
        </Grid>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="搜索：" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" Height="28" Margin="0,0,10,0" Padding="3"/>
            <Button Content="查询" Command="{Binding SearchCommand}" Width="60" Height="28" Margin="0,0,10,0"/>
            <Button Content="重置" Command="{Binding ResetCommand}" Width="60" Height="28"/>
        </StackPanel>
        
        <DataGrid Grid.Row="2" ItemsSource="{Binding Members}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" BorderBrush="#ddd">
            <DataGrid.Columns>
                <DataGridTextColumn Header="编号" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="120"/>
                <DataGridTextColumn Header="电话" Binding="{Binding Phone}" Width="150"/>
                <DataGridTextColumn Header="等级" Binding="{Binding Level}" Width="100"/>
                <DataGridTextColumn Header="注册日期" Binding="{Binding RegisterDate, StringFormat=yyyy-MM-dd}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
